import Pingback from 'assets/js/pingback';
import Config from 'assets/js/config';

var Rxports = {
	tapType : ('ontouchend' in window) ? 'touchend' : 'click',			// 确定用户单击操作时的类型
	/**
	 * 获取url中的参数
	 * @param  {[type]} s [description]
	 * @return {[type]}   [description]
	 */
	a: function(s){
			
	},

	
}
	
module.exports = Rxports



































